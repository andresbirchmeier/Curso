'use client';
import React from 'react';
import CreatePlaylist from '@/components/CreatePlaylist';

const Page = () => {
  const initialData = undefined;

  return <CreatePlaylist initialData={initialData} />;
};

export default Page;
